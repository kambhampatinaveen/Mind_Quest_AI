services:
  mongo:
    image: mongo:7
    container_name: mindquest-mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - mindquest_mongo:/data/db

volumes:
  mindquest_mongo:
